name: "Test Action"

on:
  push:
    branches: dev
    
defaults:
  run:
    shell: bash
    
jobs:
  test-job:
    env:
      CREATE_PR: ${{ false }}
    runs-on: ubuntu-latest
    steps:    
      - name: Final step
        if: ${{ env.CREATE_PR }}
        run: echo "Creating the PR"
