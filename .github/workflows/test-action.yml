name: "Test Action"

on:
  push:
    branches: dev
    
defaults:
  run:
    shell: bash
    
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CREATE_PR='yes'" >> $GITHUB_ENV
      - name: Final step
        if: ${{ env.CREATE_PR == 'yes' }}
        run: echo "Creating the PR"
